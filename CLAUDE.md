# flogvit-coder

CLI tool that orchestrates AI coding agents. Built with Bun + TypeScript.

## Development

- `bun test` to run tests
- `bun run src/cli.ts` to run the CLI
- Uses `@iarna/toml` for config parsing
- Uses `gh` CLI for GitHub integration
